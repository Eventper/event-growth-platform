Planner Dashboard sidebar restructure — link to Group Portal canonical, don't replicate.

Architectural rule:
Planning App handles EVENT operations. Group Portal is the system of record for business management (finance, projects, calendar, notifications). Planner Dashboard sidebar items that are NOT event-specific should mirror/link to Group Portal canonical — not run their own version.

Sidebar changes — current 13 items reclassified:
STAYS — event-specific tools (canonical on Planning App):

Dashboard (Planner Dashboard itself)
Create Event (the form, used by Director / Ololade / Faye + accessed via bridge from Group Portal project workspace)
Onboarding & Training (event tools training for Nigeria team)
Prospect Finder (autonomous outreach agent — this is canonical here, don't move)
Lead Management (feeds Prospect Finder)
Vendor / Decor tools
Guest Management
Run Sheet / Event Day

CHANGES — link or mirror to Group Portal canonical:

Budget / Finance — restrict visibility, send creates to Group Portal Outbound Queue


Visible only to: DIRECTOR, MARKETING_GROUP (Ololade Eddo), COUNTRY_MANAGER_NIGERIA (Faye)
Hidden for ALL other roles (Nigeria event team, Erikan, Mariam, Frank, Bidemi, Mercy, Omotolani, Idris, Oluwafisayomi, Oluwatofunmi, Paul, Abraham, Prab, Lara, Juliet, Jastin, Shravani)
The 3 visible roles see "Create Invoice" button only
Clicking "Create Invoice" opens invoice form on Planning App
On Save → invoice DOES NOT fire. Instead POST to Group Portal endpoint /api/group-portal/inbox/invoice-request with payload: { from_user, project_id, event_id, line_items, amount, currency, client, notes, source: "planning_app" }
Show confirmation banner on Planning App: "Invoice queued for Director approval — visible in Group Portal Outbound Queue"
Group Portal owns the canonical invoice + sending. Planning App is intake-only for the 3 authorised users.
Reading existing invoices on Planning App: NOT allowed. Director / Ololade / Faye view canonical invoices on Group Portal Finance tile, NOT here.


Calendar — sync entries up to Group Portal Group Calendar


Calendar entries created on Planning App (event-specific deadlines, run sheet items, vendor schedules) sync to Group Portal Group Calendar via API
Group Portal Group Calendar is the canonical view for Director / cross-company visibility
Planning App Calendar shows event-only entries; Group Calendar shows everything across all 5 companies
Sync mechanism: on every Planning App calendar entry create/edit, POST to Group Portal /api/group-portal/calendar/sync


Notifications — link to Group Portal notification stream


Notifications generated by Planning App actions (event created, run sheet updated, vendor confirmed) ALSO post to Group Portal notification stream
Director / managers can see all Planning App-generated notifications from Group Portal
Sync mechanism: on every notification fire on Planning App, POST to Group Portal /api/group-portal/notifications/feed


Project Management — REMOVE from Planning App sidebar entirely


Project management lives on Group Portal exclusively (per memory entry #2)
Planning App handles event execution within a project, NOT project management
Replace sidebar item with: "Projects (managed in Group Portal)" — clicking bridges back to Group Portal project workspace via reverse bridge
Reverse bridge: GET /api/bridge/group-portal/redeem with token signed on Planning App side, lands user back on Group Portal /portal/[company]/projects


Reports / Admin tools — split


Event-specific reports (post-event analytics, guest attendance, vendor performance) → STAY on Planning App
Admin tools (user management, role config, audit logs, system settings) → REMOVE, link to Group Portal Admin


Required confirmations:
a. The 3 Finance-visible roles (DIRECTOR / MARKETING_GROUP / COUNTRY_MANAGER_NIGERIA) — read role from bridge JWT, scope visibility per role
b. Confirm Group Portal endpoints exist for receiving:

/api/group-portal/inbox/invoice-request
/api/group-portal/calendar/sync
/api/group-portal/notifications/feed
Reverse bridge: GET /api/bridge/group-portal/redeem

If any don't exist, coordinate with Group Portal Replit to build (separate paste being sent to them now).
c. SAAS-PAUSED quarantine on /create-event remains intact for SaaS tenant context — bridge users from Group Portal still land on /planner-dashboard, NOT /create-event directly. Create Event sidebar item on Planning App is for authenticated bridged users only.

Sequence:

Audit current Planner Dashboard sidebar — confirm which 13 items render today, which roles see what
Implement role-scoped sidebar visibility (Finance restricted to 3 roles, Project Management removed for all)
Build Invoice intake → Group Portal Outbound Queue handoff
Build Calendar sync → Group Portal Group Calendar
Build Notifications sync → Group Portal notification feed
Build reverse bridge for "Projects (managed in Group Portal)" link
Production test as each role with screenshots

Estimate: 1.5 working days.

Standing rules apply. Memory entries 1-11 hold locked architecture. Don't delete the Create Event flow; restrict access. Don't break SAAS-PAUSED quarantine on /create-event. Production click-through verification before declaring shipped.
Brief me at each milestone. Don't proceed to next step until previous confirmed.